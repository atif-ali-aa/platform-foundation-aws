# scripts/

Operational helper scripts for working with this repository (bootstrap,
state migration helpers, local validation wrappers).

Nothing lives here yet. This directory is scaffolded ahead of the
`environments/` and `examples/` milestones, which are where scripts like
`bootstrap-backend.sh` (creating the S3/DynamoDB remote state backend)
will become necessary.
